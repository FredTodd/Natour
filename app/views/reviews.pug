extends layout
block content
    - console.log(data)
    .main-content 
    h1 All Reviews 
    table(border=1)
        for item in data 
            tr  
                td #{item.review_id}
                td 
                    a(href='/student-single/' + item.review_id) #{item.review}